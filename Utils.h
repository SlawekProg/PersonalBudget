#ifndef UTILS_H
#define UTILS_H
#include <iostream>
#include <string.h>

using namespace std;

class Utils
{
public:
    char getCharacter();
    string readLine();
    bool validateInput(string input);
};

#endif
